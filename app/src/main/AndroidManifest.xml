<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.opcion.juan.pizzeria">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".registroActivity" />
        <activity android:name=".productosActivity" />
        <activity android:name=".activityPizzas" />
        <activity android:name=".activityBebidas" />
        <activity android:name=".activityPostres" />
        <activity android:name=".CarritoActivity" />
        <activity android:name=".recuperarActivity"></activity>
    </application>

</manifest>